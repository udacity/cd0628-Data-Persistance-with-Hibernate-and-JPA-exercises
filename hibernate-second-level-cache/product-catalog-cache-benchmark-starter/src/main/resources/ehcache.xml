<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xsi:schemaLocation="http://www.ehcache.org/v3
                            http://www.ehcache.org/schema/ehcache-core-3.10.xsd">

    <!--
    ============================================================
    TODO 2: Define the product cache region.

    Inside this <config> element, add a <cache> block:

      <cache alias="com.udacity.cache.Product">
          <expiry>
              <ttl unit="minutes">10</ttl>
          </expiry>
          <heap unit="entries">100</heap>
      </cache>

    The alias MUST match the fully-qualified entity class name
    so Hibernate uses the right region for Product entries.
    ============================================================
    -->

</config>